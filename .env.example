# GroundStack local development configuration.
# Copy to .env. Never commit .env or place real secrets in this example.

# Required local application settings
APP_NAME=GroundStack
APP_ENV=development
ENVIRONMENT=local
LOG_LEVEL=INFO
API_HOST=0.0.0.0
API_PORT=8000
PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
TRUSTED_HOSTS=localhost,127.0.0.1,testserver,test
DEV_AUTH_BYPASS_ENABLED=true
DOCS_ENABLED=true

# Required PostgreSQL and pgvector settings
POSTGRES_USER=groundstack
POSTGRES_PASSWORD=groundstack
POSTGRES_DB=groundstack
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://groundstack:groundstack@localhost:5432/groundstack
DATABASE_DIRECT_URL=
DB_SSL_REQUIRED=false
DB_POOL_SIZE=5

# Required document processing and semantic retrieval settings
EMBEDDING_PROVIDER=sentence_transformers
EMBEDDING_MODEL_NAME=BAAI/bge-small-en-v1.5
EMBEDDING_DIMENSION=384
EMBEDDING_BATCH_SIZE=16
EMBEDDING_DEVICE=auto
CHUNK_TARGET_TOKENS=350
CHUNK_OVERLAP_TOKENS=60
MAX_INGESTION_FILE_SIZE_BYTES=10485760
URL_INGESTION_ALLOWED_DOMAINS=docs.example.com
MAX_RETRIEVAL_QUERY_LENGTH=1200
RETRIEVAL_CANDIDATE_LIMIT=20
RETRIEVAL_FINAL_TOP_K=5
RETRIEVAL_MAX_TOP_K=8
RETRIEVAL_MAX_VECTOR_DISTANCE=0.45
RETRIEVAL_ALGORITHM_VERSION=semantic-vector-v1
PERSIST_RETRIEVAL_QUERIES=false
RETRIEVAL_DEBUG_ENABLED=false
RETRIEVAL_TIMEOUT_SECONDS=30

# Required generation provider settings.
# For Ollama, LLM_API_KEY remains empty. For a hosted OpenAI-compatible
# provider, set LLM_PROVIDER=openai_compatible and enter the key only in .env.
LLM_PROVIDER=ollama
LLM_MODEL=llama3.2:3b
LLM_BASE_URL=http://localhost:11434
LLM_API_KEY=
LLM_REQUEST_TIMEOUT_SECONDS=120
LLM_TEMPERATURE=0.1
LLM_TOP_P=0.9
LLM_MAX_OUTPUT_TOKENS=700
LLM_CONTEXT_WINDOW=8192
LLM_PREWARM=false
LLM_MAX_RETRIES=1
GENERATION_PROMPT_VERSION=grounded_answer/v1
STORE_GENERATION_PROMPTS=false
MAX_CONVERSATION_HISTORY_MESSAGES=8

# Optional local safety and capacity controls
CHAT_RATE_LIMIT_PER_MINUTE=20
FEEDBACK_RATE_LIMIT_PER_MINUTE=30
GENERATION_CONCURRENCY=2
RETRIEVAL_CONCURRENCY=4
MODEL_QUEUE_TIMEOUT_SECONDS=3
MAX_REQUEST_BODY_BYTES=12582912
SESSION_COOKIE_NAME=groundstack_session
SESSION_COOKIE_SECURE=false
SESSION_COOKIE_SAMESITE=lax
SESSION_LIFETIME_SECONDS=3600

# Optional OIDC settings for a secured hosted deployment
OIDC_ISSUER_URL=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_AUDIENCE=
OIDC_SCOPES=openid profile email
OIDC_ROLE_CLAIM=roles
OIDC_ADMIN_ROLE=admin
OIDC_ALLOWED_ALGORITHMS=RS256

# Optional controlled public-demo settings. Local counters are process-scoped;
# this mode is intended only for a single API instance.
ALLOW_ANONYMOUS_DEMO=false
DEMO_CHAT_ENABLED=true
DEMO_ALLOWLIST_MODE=false
DEMO_ALLOWLIST_IPS=
DEMO_REQUEST_LIMIT_PER_MINUTE=8
DEMO_DAILY_QUESTION_LIMIT=100
DEMO_DAILY_TOKEN_LIMIT=15000
DEMO_MAX_QUESTION_LENGTH=600
DEMO_MAX_CONTEXT_TOKENS=2500
DEMO_PROVIDER_FAILURE_THRESHOLD=5
DEMO_PROVIDER_FAILURE_WINDOW_SECONDS=300
DEMO_UPLOAD_LIMIT_BYTES=0
DEMO_MAX_CONVERSATIONS=5

# Deterministic provider controls used only by automated tests
FAKE_LLM_FIRST_TOKEN_DELAY_MS=25
FAKE_LLM_TOKEN_RATE_PER_SECOND=80
FAKE_LLM_TOTAL_TOKENS=80
FAKE_LLM_FAILURE_MODE=none
