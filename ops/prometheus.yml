global:
  scrape_interval: 15s

scrape_configs:
  - job_name: groundstack-api
    metrics_path: /api/v1/metrics
    authorization:
      type: Bearer
      credentials_file: /run/secrets/metrics_token
    static_configs:
      - targets: ["api:8000"]
